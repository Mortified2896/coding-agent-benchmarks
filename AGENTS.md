# Project Agent Instructions

This file is loaded by AI coding assistants (and human contributors) when working in this repository.

## Coding Worker Routing

For coding tasks, prefer delegating implementation through OpenCode using `opencodebench-opencode` and follow `docs/current-openbench-model-routing.md`.

Do not use direct file edits by default for implementation work unless the user explicitly asks or the change is trivial and does not need benchmarking.
