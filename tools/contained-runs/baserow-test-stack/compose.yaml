services:
  baserow:
    image: docker.io/baserow/baserow:1.30.1
    container_name: contained-runs-baserow-test
    ports:
      - "127.0.0.1:18080:80"
    environment:
      BASEROW_PUBLIC_URL: "http://127.0.0.1:18080"
    volumes:
      - baserow_data:/baserow/data
volumes:
  baserow_data: {}
